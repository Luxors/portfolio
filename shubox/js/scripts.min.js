function play() {
	vid.paused ? (vid.play(), playButton.className = "play-btn-hide") : (vid.pause(), playButton.className = "video__play")
}

function stop() {
	vid.stoped && (vid.stop(), playButton.className = "video__play")
}

$(document).ready(function () {

	if(/Android|BlackBerry|iPhone|iPad|iPod|webOS|IEMobile|Opera Mini/i.test(navigator.userAgent) === false) {
		$(".parallax-layer").parallax({
			mouseport: ".intro",
			width: 410,
			height: 470
		});
	}

	if(/Android|BlackBerry|iPhone|iPad|iPod|webOS|IEMobile|Opera Mini/i.test(navigator.userAgent)) {
		setTimeout(function() {
			$('#downloadMobile').arcticmodal();
		 },30000);
	}

	function onOpen()
	{
		if (/iPhone|iPad|iPod/i.test(navigator.userAgent))
			$('.page-wrapper').hide();
	}
	function onClose()
	{
		if (/iPhone|iPad|iPod/i.test(navigator.userAgent))
			$('.page-wrapper').show();
	}

	var cfg = {
	    beforeOpen: function(data, el) {
	        onOpen();
	    },
	    beforeClose: function(data, el) {
	        onClose();
	    }
	};

	$(".intro__modal").click(function (o) {
		$("#popupZvonok").arcticmodal()
	}), $(".about__modal").click(function (o) {
		$("#popupZakazat").arcticmodal()
	}), $(".promo-modal").click(function (o) {
		$("#popupSozdat").arcticmodal()
	}), $(".product__modal").click(function (o) {
		$("#popupProduct").arcticmodal()
	}), $('a[href^="#"], *[data-href^="#"]').on("click", function (o) {
		o.preventDefault();
		var t = $(this).attr("data-href") ? $(this).attr("data-href") : $(this).attr("href");
		$("html,body").stop().animate({
			scrollTop: $(t).offset().top
		}, 1e3)
	}), $("#mobileMenuButton").click(function () {
		$(this).toggleClass("mobileMenuOpen"), $(".header__menu").slideToggle(300)
	}), window.matchMedia("(max-width: 767px)").matches && $(".menu__link").click(function () {
		$(".header__menu").slideToggle(300), $("#mobileMenuButton").removeClass("mobileMenuOpen")
	}), $("#gallerySlider").slick({
		dots: !1,
		prevArrow: !1,
		nextArrow: ".gallery__btn",
		infinite: !0,
		slidesToShow: 4,
		slidesToScroll: 4,
		responsive: [{
			breakpoint: 768,
			settings: {
				slidesToShow: 1,
				slidesToScroll: 1,
				infinite: !0
			}
		}]
	}), $("#btnGlossy").click(function () {
		$(".product__price-inner").text("20.000 руб"), $(this).css({
			color: "#FFF",
			"background-color": "#363636"
		}), $("#btnMatted").css({
			color: "#363636",
			"background-color": "#FFF",
			border: "2px solid #363636"
		})
	}), $("#btnMatted").click(function () {
		$(".product__price-inner").text("10.000 руб"), $(this).css({
			color: "#FFF",
			"background-color": "#363636"
		}), $("#btnGlossy").css({
			color: "#363636",
			"background-color": "#FFF",
			border: "2px solid #363636"
		})
	}), $(".parallax-layer").parallax({
		mouseport: ".intro__banner",
		width: 410,
		height: 470
	})
});

var overlay = document.getElementById("videoBox"),
	vid = document.getElementById("video"),
	playButton = document.getElementById("playBtn");
overlay.addEventListener ? overlay.addEventListener("click", play, !1) : overlay.attachEvent && overlay.attachEvent("onclick", play);
