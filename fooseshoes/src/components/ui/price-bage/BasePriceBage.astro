---
import './styles.scss';

interface Props {
  price: string;
  textSize?: 'sm' | 'md' | 'lg';
}


const { price, textSize = 'lg' } = Astro.props;
---

<div class="base-price-bage">
  <span class={`base-price-bage__text base-price-bage__text--${textSize}`}>${price}</span>
</div>